name: Build & Deploy (Railway)

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    outputs:
      reset_db: ${{ steps.check_migrations.outputs.reset_db }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          submodules: recursive

      - name: Detect migration changes
        id: check_migrations
        run: |
          if git diff --name-only HEAD~1 HEAD | grep -q "ManagementService/Infrastructure/Migrations"; then
            echo "reset_db=true" >> $GITHUB_OUTPUT
          else
            echo "reset_db=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Restore dependencies
        run: dotnet restore ManagementService/API/API.csproj

      - name: Build
        run: dotnet build ManagementService/API/API.csproj --configuration Release --no-restore

      - name: Login Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Build Docker image
        run: |
          docker build \
            -t longdong123/shroomfall-managementms:latest \
            -f ManagementService/Dockerfile .

      - name: Push Docker image
        run: docker push longdong123/shroomfall-managementms:latest


  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli

      # 1. WIPE PATH: Set variable to true (triggers deploy), then immediately turn it off for the future
      - name: Redeploy Management Service with DB Wipe
        if: ${{ needs.build.outputs.reset_db == 'true' }}
        run: |
          # This saves WIPE_DB=true and instantly triggers the wipe deployment
          railway variables set WIPE_DB=true --service managementms-api.railway.internal
          
          # Wait 15 seconds to ensure Railway snapshots the "true" variable for the active deployment...
          sleep 15
            
          # ...then safely flip it back to "false" so future crashes or normal deploys don't wipe the DB!
          railway variables set WIPE_DB=false --service managementms-api.railway.internal --skip-deploys
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

        # 2. NORMAL PATH: Only runs if a database reset was NOT requested
      - name: Redeploy Management Service (Normal)
        if: ${{ needs.build.outputs.reset_db != 'true' }}
        run: railway redeploy --service managementms-api.railway.internal --yes
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

